#!/bin/sh

# Setup heroku application remotes
if heroku auth:whoami &> /dev/null; then
  if heroku apps:info --app pl-gh-pr-stats-staging &> /dev/null; then
    git remote add staging git@heroku.com:pl-gh-pr-stats-staging.git || true
    git config heroku.remote staging
    echo 'You are a collaborator on the "pl-gh-pr-stats-staging" Heroku app'
  else
    echo 'Ask for access to the "pl-gh-pr-stats-staging" Heroku app'
  fi

  if heroku apps:info --app pl-gh-pr-stats-production &> /dev/null; then
    git remote add production git@heroku.com:pl-gh-pr-stats-production.git || true
    echo 'You are a collaborator on the "pl-gh-pr-stats-production" Heroku app'
  else
    echo 'Ask for access to the "pl-gh-pr-stats-production" Heroku app'
  fi
else
  echo 'You need to login to heroku. Run "heroku login"'
fi
